<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Candidato</title>
    <link rel="stylesheet" href="/css/styles.css"> <!-- CSS opcional -->
</head>
<body>

    <h1>Editar Candidato</h1>

    <form action="/candidatos/editar/<%= candidato.id %>" method="POST"> <!-- Mudança aqui -->
        <label for="nome">Nome do Candidato:</label>
        <input type="text" id="nome" name="nome" value="<%= candidato.nome %>" required>
        
        <label for="partido">Partido:</label>
        <select id="partido" name="partido" required>
            <option value="">Selecione um partido</option>
            <% partidos.forEach(partido => { %>
                <option value="<%= partido.id %>" <%= partido.id === candidato.partido_id ? 'selected' : '' %>><%= partido.nome %></option>
            <% }); %>
        </select>


        <label for="numero">Número do Candidato:</label>
        <input type="number" id="numero" name="numero" value="<%= candidato.numero %>" required>
        
        <button type="submit">Salvar Alterações</button>
    </form>
</body>
</html>
